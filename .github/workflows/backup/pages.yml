name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build documentation
        run: |
          # Create a simple documentation site
          mkdir -p docs-site
          cp -r docs/* docs-site/ || true
          cp README.md docs-site/
          cp .github/SPONSORS.md docs-site/
          cp .github/security.yml docs-site/
          
          # Create index.html
          cat > docs-site/index.html << 'HTML_EOF'
          <!DOCTYPE html>
          <html lang="pt-BR">
          <head>
              <meta charset="UTF-8">
              <meta name="viewport" content="width=device-width, initial-scale=1.0">
              <title>ControlFin - Documentação</title>
              <style>
                  body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; max-width: 800px; margin: 0 auto; padding: 20px; }
                  h1, h2, h3 { color: #2d3561; }
                  code { background: #f5f5f5; padding: 2px 4px; border-radius: 3px; }
                  pre { background: #f5f5f5; padding: 15px; border-radius: 5px; overflow-x: auto; }
                  a { color: #00d9ff; text-decoration: none; }
                  a:hover { text-decoration: underline; }
              </style>
          </head>
          <body>
              <h1>🏦 ControlFin</h1>
              <p>Progressive Web App para controle de finanças pessoais e compartilhadas.</p>
              
              <h2>📚 Documentação</h2>
              <ul>
                  <li><a href="README.md">README Principal</a></li>
                  <li><a href="SPONSORS.md">Apoie o Projeto</a></li>
                  <li><a href="security.yml">Política de Segurança</a></li>
              </ul>
              
              <h2>🚀 Início Rápido</h2>
              <pre><code># Clone o repositório
git clone https://github.com/luisfelipedeoliveira/controlFin.git

# Instale as dependências
cd controlFin
npm install

# Execute em desenvolvimento
npm run dev
              </code></pre>
              
              <h2>🔗 Links Úteis</h2>
              <ul>
                  <li><a href="https://github.com/luisfelipedeoliveira/controlFin">Repositório GitHub</a></li>
                  <li><a href="https://github.com/luisfelipedeoliveira/controlFin/issues">Reportar Bug</a></li>
                  <li><a href="https://github.com/luisfelipedeoliveira/controlFin/discussions">Discussões</a></li>
              </ul>
          </body>
          </html>
          HTML_EOF

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './docs-site'

  deploy:
    environment:
      name: github-pages
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
